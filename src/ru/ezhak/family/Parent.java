/*package ru.ezhak.family;

public class Parent {
    public String name;
    private Reaction reaction;

    public Parent(String name, Reaction reaction) {
        this.name = name;
        this.reaction = reaction;
    }

    public void reaction (Student student) {
        if (student.getMarks().getLast() == 2 || student.getMarks().getLast() == 3) {
            System.out.printf("%s: %s, %s, получил %d\n", this.name, reaction.react(), student.name, student.getMarks().getLast());
        }
    }

    public void setReaction(Reaction reaction) {
        this.reaction = reaction;
    }
}*/
